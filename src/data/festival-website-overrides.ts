/**
 * Official websites for festivals Wikidata doesn't cover (or covers wrongly).
 * Keys are festival ids from the generated/seed list.
 */
export const FESTIVAL_WEBSITE_OVERRIDES: Record<string, string> = {
  "country-calling-uk-2026": "https://www.country-calling.com/",
};
